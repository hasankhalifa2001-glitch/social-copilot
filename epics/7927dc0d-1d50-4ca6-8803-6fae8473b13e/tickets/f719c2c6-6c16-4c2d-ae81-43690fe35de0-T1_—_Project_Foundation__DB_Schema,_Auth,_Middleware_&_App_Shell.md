# T1 — Project Foundation: DB Schema, Auth, Middleware & App Shell

Ticket ID: f719c2c6-6c16-4c2d-ae81-43690fe35de0

Goal:
Lay groundwork: database schema, authentication, middleware, and a base app shell/layout.

Deliverables:
- DB schema draft (Postgres/Prisma)
- Auth integration plan (Clerk / OAuth fallback)
- Middleware for request handling and RBAC
- Base `app/layout.tsx` and global styles

Next steps:
1. Add `prisma/schema.prisma` draft and migration notes.
2. Wire Clerk auth client and server middleware stubs.
3. Provide `app/layout.tsx` shell with header/sidebar placeholders.
